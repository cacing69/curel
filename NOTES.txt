- added 8 new Phase 3 items (Wave 2): batch curl import, simple assertions, request notes, .curlrc support, proxy config, folder-level env override, network throttle presets, duplicate with env switch
- updated PLAN.md Phase 3 feature list + workspace structure
- updated memory/product_roadmap.md to match PLAN.md
- updated memory/phase3_expansion.md with Wave 2 details and rationale
- updated memory/SYNC.md current state to reflect new items
- synced all memory files to claude memory directory
- created bloats/00.md with nice-to-have + underrated feature wishlist, added bloats/ to .gitignore
- implemented duplicate request with env switch: added targetEnv field to RequestMeta, env picker dialog in drawer context menu
- implemented request notes (.notes.md sidecar): added readNotes/writeNotes to RequestService, notes editor dialog in drawer context menu
- implemented batch curl import: added menu item in more_menu, paste dialog in home_page with auto-split curl commands
- implemented curl config support (.curlrc): added readCurlrc to RequestService, merge .curlrc flags before curl execution in HomeActions
- fixed integration gaps: deleteRequest, renameRequest, duplicateRequest now handle all sidecar files (.meta.json, .notes.md) via _sidecarPathsFor helper
- implemented Bruno Import/Export adapter: _BruParser for .bru format (dictionary/text/array blocks), import .bru → curl, export curl → .bru, registered in AdapterRegistry
- registered VscodeRestAdapter in AdapterRegistry — .http/.rest file import/export now available alongside Bruno, Postman, Insomnia, Hoppscotch, Curel Native
- implemented cookie jar management: named jars per project (.cookiejar/ dir), CookieEntry model with RFC 6265 domain/path/secure matching, CookieJarService with Netscape format import, auto-inject cookies via -b flag before execute, auto-capture Set-Cookie from responses, cookie jar dialog UI with add/edit/delete/import, env bar cookie button, more menu entry
