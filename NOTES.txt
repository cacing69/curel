- revert resolved preview back to original Dialog approach
- simplify global env UI: flat variable list instead of named environments, auto-create default env, add var button directly
- replace getApplicationDocumentsDirectory with getApplicationSupportDirectory to remove "app_flutter" from workspace path
- add workspace import/export: WorkspaceService bundles all projects, requests, envs into single JSON file
- add project-level export/import: export per project from popup menu, import from footer
- add type field (env/project/workspace) to all export formats with validation on import to prevent wrong file type
- fix workspace switching: sync project list from filesystem on change, force HomePage rebuild via key
- add image compression before feedback submit (quality 80%, maxWidth 720) using flutter_image_compress
- redesign feedback page: compact layout with inline category tabs, collapsed optional details, tighter spacing
